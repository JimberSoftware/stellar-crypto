image: node:alpine

stages:
  - build
  - test

Build:
  stage: build
  before_script:
    - yarn install 
  script:
    - yarn build

Test:
  stage: test
  before_script: 
    - yarn install 
  script:
    - yarn test

